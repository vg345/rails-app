<div class="home-callout text-center container-fluid">
  <h2>Get a healthier life for yourself and your loved ones.</h2>
</div>
<div class="container home-sign-up">
  <div class="row">
    <div class="col-md-4">
      <div class="well">
        <h1 class="text-center">Start today.</h1>
        <% if user_signed_in? %>
          <%= link_to "Create Profile", '#', class: 'btn btn-warning btn-lg btn-block' %>
        <% else %>
          <%= link_to "Sign Up", new_user_registration_path, class: "btn btn-warning btn-lg btn-block" %>
        <% end %>
      </div>
    </div>
  </div>
</div>